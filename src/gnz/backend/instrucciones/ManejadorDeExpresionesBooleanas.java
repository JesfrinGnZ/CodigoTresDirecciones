/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package gnz.backend.instrucciones;

import gnz.backend.cuarteto.ManejadorDeCuartetos;
import gnz.backend.manejoDeDatos.DatoCodigo;
import gnz.backend.manejoDeDatos.Operacion;

/**
 *
 * @author jesfrin
 */
public class ManejadorDeExpresionesBooleanas {
/*
    public static void crearIf(Operacion operacion,DatoCodigo dato1, DatoCodigo dato2, ManejadorDeCuartetos manCuarteto) {
        String temp1;
        String temp2;
        if (dato1.getTemporal() == null) {//Solo es un dato, por lo que se crea un temporal
            temp1 = "t" + manCuarteto.generarTemporal();
        } else {
            temp1 = dato1.getTemporal();
        }
        if (dato2.getTemporal() == null) {//Solo es un dato
            temp2 = "t" + manCuarteto.generarTemporal();
        } else {
            temp2 = dato2.getTemporal();
        }
        manCuarteto.crearCuartetoIf(operacion, temp1, temp2);
    }
*/
}
